PORT=5000
MONGODB_URI=
AUTH_SECRET=
# Comma-separated frontend origins allowed for credentialed CORS.
# Same-origin deploys (UI + API on one host) do not need this.
CLIENT_ORIGIN=http://localhost:5173
CORS_ORIGINS=
